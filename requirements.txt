# Core speech and text processing
SpeechRecognition
pyttsx3
pyaudio

# Windows-specific text-to-speech (only for Windows)
pywin32

# Web scraping and browser control
wikipedia

# Streamlit for web interface
streamlit
streamlit-webrtc

# Audio/video processing
av

# Scientific computing
numpy

# Natural Language Processing
nltk
textblob
spacy

# Additional dependencies that might be needed
requests
urllib3

# Note: You may also need to download NLTK data and spaCy models after installation
# Run these commands after pip install:
# python -m nltk.downloader punkt stopwords wordnet averaged_perceptron_tagger maxent_ne_chunker words
# python -m spacy download en_core_web_sm